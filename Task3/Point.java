import java.util.*;

public class Point {
    protected int x;
    protected int y;

    public Point() {
        this.x = (int)(Math.random()*30);
        this.y = (int)(Math.random()*30);
    }
}
